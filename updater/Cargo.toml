[package]
name = "updater"
version = "0.2.0"
edition = "2024"

[dependencies]
tokio = { version = "*", default-features = false, features = ["rt-multi-thread", "macros", "process"] }

[profile.release]
opt-level = 3
strip = true
codegen-units = 1
lto = "fat"
panic = "abort"
